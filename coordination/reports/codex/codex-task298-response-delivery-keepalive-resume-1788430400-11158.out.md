- **LOW — Lifecycle regression coverage omits true pipelining and close-request variants.** The reuse test sends its second request only after fully reading the first response ([test_tts_server.py](/C:/projects/ai-nd-co/repos/agent-tools/tests/test_tts_server.py:327)), so it would not catch code that opportunistically consumes an already-pipelined request. It also does not protect the HTTP/1.0 or `Connection: close` behavior. Add raw-socket cases that send both requests in one `sendall`, plus successful WAV requests using HTTP/1.0 and explicit close. The current implementation passed all three scenarios in a local probe, so this is a regression-coverage gap, not a present protocol defect.